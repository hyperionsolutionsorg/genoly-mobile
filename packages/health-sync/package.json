{
  "name": "@genoly/health-sync",
  "version": "0.0.1",
  "private": true,
  "description": "HealthKit + Health Connect adapter interfaces for the Genoly mobile app. Implementation lands in Phase 1. All health-reading code lives here only — never in screen components.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "UNLICENSED",
  "dependencies": {
    "@genoly/types": "*"
  }
}
